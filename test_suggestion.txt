The suggestion is to add logic to "clean up not useful data, keep useful stuff only" during the nightly sleep cycle, rather than relying purely on access frequency and duration.
Since the backend uses an LLM Agent for evaluations (`domain.Agent` / `usecase.Agent`), we could potentially use the LLM to evaluate the usefulness of a memory vector before pruning it, or we can use the LLM to consolidate memories.
Wait, SDD-Constitution.md says:
"The Sleep Cycle (Nightly Daemon):
1. Consolidates raw VFS chat logs into facts/preferences.
2. Evaluates existing vectors based on Access Frequency and Time Since Last Recall. Irrelevant data is permanently deleted unless flagged as Core Memory.
3. Purges raw daily logs."

The user's comment is: "the pruning should not only be duration based. there should be some logic to clean up not usefull data . keep useful stuff only and should happen nightly. suggestion?"
Since they are asking for a suggestion on how to do this, I can propose using the LLM Agent to evaluate the usefulness of the data during the sleep cycle. The LLM could be given the facts/preferences and asked to flag them as irrelevant/useful, or consolidate them. Or we could use the existing LLM Agent to score the usefulness of vectors before deletion.

Let me draft a reply:
"That's a great point. We can leverage our existing LLM `Agent` to evaluate the actual semantic usefulness of the data during the Nightly Sleep Cycle.

Currently, the daemon uses heuristic thresholds (Access Frequency and Time Since Last Recall) as a first-pass filter. To make it smarter, we can inject the `Agent` into the `NightlySleepCycle` and add an LLM-driven evaluation step.

Before deleting the heuristically selected vectors, the daemon could prompt the LLM:
`'Evaluate this memory: "{content}". Is it a critical long-term fact/preference, or is it obsolete/useless data? Respond with KEEP or DISCARD.'`

This way, we combine the efficiency of duration/frequency filtering with the semantic understanding of the LLM to ensure we only keep truly useful stuff. We could also add a similar step to consolidate raw chat logs into concise facts before pruning the logs. Would you like me to implement this LLM evaluation logic into the daemon?"
